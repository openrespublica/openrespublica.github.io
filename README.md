# 🏛️ OpenResPublica: Immutable Record Automation & Verification 
An immutable registration and verification node designed to run on Android (Termux).

## 🚀 Architecture
- **Engine:** Python/Flask (API Logic)
- **Vault:** immudb (Immutable Ledger)
- **Gatekeeper:** NGINX (Static Serving & Reverse Proxy)
- **Frontend:** HTML5/JS (Decentralized Verification)

## 🛠️ Deployment
1. Install Termux
2. Clone this repo
3. Run `./setup.sh` (We can build this next!)
